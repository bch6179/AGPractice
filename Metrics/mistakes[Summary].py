#dp[i]+=1, and return dp[i] , mistake to return dp[i]+1
# BTS Iterator if self.hasNext(): #Mistake need to bracket for function, need self
# need to return t.val
#. num[i] after change nums to num

#. return nums for expecting a length

#. Capitalize module list
sCounter = Counter(s[:len(p)-1])
        # for i in range(0, w-1):
        #     sCounter[s[i]] += 1  #Mistake sCounter not defined if not declare above; and w-1 index error,but not the case use slice initiliazation

