k｛
  p = a[n-1];
  s = 0;
  while(i < n-1) {
    if (a[i] < p) {
      swap(a[i], a[s]);
      s++;
    }

  }
    swap(a[s], a[n-1]);

   // 2 1 5  3
  return s;
}
｝
  k1 {
     e = 0 ,  g= n-1;
     s = 0;
     while(e <= g)
     if (a[i] > p) {
        swap(a[--g] ,a[i]);
      }
      else if (==) {
         //swap(a[e] ,a[i]);
         e++;
      }
      else if (<) {
        swap(a[e++],a[s++]);
      }
  }